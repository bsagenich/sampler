# Starter pipeline
- task: MSBuild@1
  inputs:
    solution: '- task: FortifySCA@6
  inputs:
    licenseFile: 'D:\SPI\Fortify-SPI\fortify v3 installs\20.1\fortify.license'
    runBuildTranslate: true
    applicationType: 'dotnet'
    fortifyProjects: 'C:\Program Files\Fortify\Fortify_SCA_and_Apps_20.1.0\Samples\advanced\csharp\VS2019\Sample1\*.sln'
    fortifyBuildId: 'Test Sample'
    runFortifyRulepackUpdate: false
    runFortifyClean: true
    scaVerbose: false
    scaDebug: false
    runFortifyScan: true
    fortifyScanType: 'Local'*.sln'
    clean: true
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
